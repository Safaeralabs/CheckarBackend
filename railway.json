{
  "$schema": "https://railway.app/railway-schema.json",
  "deploy": {
    "startCommand": "python manage.py migrate --noinput && python manage.py collectstatic --noinput && python manage.py seed_demo && gunicorn config.wsgi --bind 0.0.0.0:$PORT --workers 2 --timeout 60",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
